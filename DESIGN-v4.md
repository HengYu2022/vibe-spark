# Design: Vibe Spark v3 — 灵感不找你，你不用找灵感

Generated by /office-hours on 2026-04-03
Branch: feat/v2-inspiration-engine
Repo: vibe-spark
Status: DRAFT
Mode: Builder
Supersedes: DESIGN-v3.md

## 问题描述

v2 解决了「灵感不个性化」的问题（从静态库匹配变成 AI 根据用户背景实时生成）。但 v2 仍然需要用户**主动触发** /vibe-spark。

用户的真实场景：他每天在 Claude Code 里工作，做 research、写代码、尝试新东西。在这个过程中，他可能反复做同一类事情却没意识到这可以变成一个项目。

**v3 的核心洞察：灵感应该在用户工作过程中自然出现，而不是用户停下来去找灵感。**

## v3 核心改变

```
v1: 用户触发 → 从库里挑            = 自动贩卖机
v2: 用户触发 → AI 个性化生成        = 个性化顾问
v3: Skill 观察你工作 → 主动建议     = 智能助手
```

## 技术方案

利用 Claude Code 的 `UserPromptSubmit` hook。

```
用户发消息 → hook 触发（每次都触发）
           → shell 脚本递增计数器（~/.vibe-spark/counter）
           → 如果是第 20 的倍数，注入一行提示给 Claude
           → Claude 根据当前对话上下文判断有没有重复模式
           → 有 → 在回复末尾加一句灵感建议
           → 没有 → 什么都不加
```

**关键设计决策：**
- **hook 只递增计数器**（一个数字），不记录任何消息内容，零隐私风险
- **Claude 自己判断模式**，它已经有完整对话上下文，不需要外部日志
- **每 20 条消息才检查一次**，不是每条。Token 成本几乎为零
- **建议以回复末尾一行的形式出现**，不打断用户工作流
- **保留 /vibe-spark 手动触发**，两种模式共存

## hook 实现

在 `.claude/settings.local.json`（或用户级 settings）中配置：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "type": "command",
        "command": "bash ~/.claude/skills/vibe-spark/hook.sh"
      }
    ]
  }
}
```

`hook.sh` 内容：

```bash
#!/bin/bash
COUNTER_FILE=~/.vibe-spark/counter
mkdir -p ~/.vibe-spark
COUNT=$(cat "$COUNTER_FILE" 2>/dev/null || echo 0)
COUNT=$((COUNT + 1))
echo $COUNT > "$COUNTER_FILE"

if [ $((COUNT % 20)) -eq 0 ]; then
  echo "【Vibe Spark】如果你注意到用户在这个 session 中重复做了类似的事 3 次以上（比如多次手动抓数据、反复写类似格式的内容、多次做同类操作），在回复末尾加一行：✨ Vibe Spark: [具体建议，说清楚做什么、为什么值得做、大概要多久]。如果没有发现重复模式，什么都不加，也不要说'我没发现重复模式'。"
fi
```

## 用户体验

### 场景 1：正常工作（大部分时间）

用户在 Claude Code 里写代码、调试、做 research。Vibe Spark 在后台静默计数。用户完全感知不到它的存在。

### 场景 2：发现模式时

用户在一个 session 里第 3 次手动抓 RSS 数据。第 20 条消息时，Claude 回复正常内容后，末尾多了一行：

```
✨ Vibe Spark: 你这个 session 里已经手动抓了 3 次 RSS 数据。
要不要做一个自动情报聚合器？大概 4 小时就能搞定。
说「/vibe-spark」我帮你展开这个方向。
```

### 场景 3：用户主动触发

用户随时可以输入 `/vibe-spark`，走 v2 的个性化生成流程。

## 前提

1. UserPromptSubmit hook 可以执行 shell 脚本，零 token 成本
2. 每 20 条消息分析一次足够发现重复模式
3. 回复末尾加建议不打扰用户
4. 只记录计数器（一个数字），零隐私风险
5. v1 灵感库 + v2 个性化生成 + v3 被动观察三层共存
6. Claude 在当前 session 有完整对话上下文，不需要外部日志来分析模式

## 三层架构

```
┌─────────────────────────────────────┐
│ v3: Hook 被动观察                    │ ← 灵感来找你
│ 每 20 条消息检查一次重复模式          │
├─────────────────────────────────────┤
│ v2: AI 个性化生成                    │ ← /vibe-spark 手动触发
│ 先了解你，再生成专属方向              │
├─────────────────────────────────────┤
│ v1: 灵感库                          │ ← few-shot 示例 + 保底
│ 40 条精选灵感                        │
└─────────────────────────────────────┘
```

## 实现范围

1. 新增 `hook.sh` 脚本
2. 新增安装时的 hook 配置说明（README）
3. SKILL.md 新增 v3 被动观察模式说明
4. 保留 v2 的所有功能（手动触发 + 个性化生成）
5. 保留 v1 的灵感库（few-shot + 保底）

## 安装变化

v1/v2 安装：只需要 `git clone`

v3 安装：`git clone` + 配置 hook（多一步）

```bash
# 1. 安装 skill
git clone https://github.com/HengYu2022/vibe-spark.git ~/.claude/skills/vibe-spark

# 2. 配置 hook（可选，不配也能用 /vibe-spark）
# 在 Claude Code 的 settings 中添加 UserPromptSubmit hook
```

hook 是可选的。不配 hook 就是 v2 体验（手动触发），配了就是 v3 体验（被动观察）。

## 建议质量边界

不限制「简单还是复杂」，限制「能不能用自然语言描述清楚」：

1. **能用一句话说清楚「我要什么」** — 不需要懂技术细节
2. **从零开始的新项目** — 不是修改现有复杂代码
3. **成功标准可见** — 看到输出就知道做没做对，不需要读代码验证

零基础 vibe coder 的能力边界比想象的宽。只要能用自然语言描述，AI 就能做。包括全栈 Web 应用、CLI 工具、API 集成、SaaS 产品。真正做不了的只有需要读懂代码才能推进的事（调试、性能优化、给别人的开源项目贡献代码）。

## 用户体验优化

1. **安装零摩擦** — install.sh 一键配置（自动写 hook 配置到 settings），用户不需要编辑 JSON
2. **首次体验** — 安装完显示欢迎语 + 两种使用方式说明
3. **噪音控制** — 排除调试/review 等非重复劳动场景，每 session 最多建议 2 次
4. **卸载干净** — uninstall.sh 清理 skill + hook 配置 + 持久化数据

## 风险

1. **误触** — Claude 可能错误判断为重复模式。缓解：要求「3 次以上」才建议，降低误报率
2. **hook 配置门槛** — 比 git clone 多一步。缓解：README 里给出一键配置命令
3. **模型差异** — 不同模型对注入指令的遵从度不同。缓解：指令写得具体，测试多个模型

## 成功标准

- 用户在正常工作中收到至少 1 次有用的灵感建议
- 建议的灵感跟用户正在做的事直接相关
- 用户的评价从「一般」变成「有用」或「优秀」

## 下一步

1. 创建 hook.sh
2. 更新 SKILL.md（新增 v3 模式说明）
3. 更新 README（hook 安装说明）
4. 测试 hook 在 Claude Code 中的实际行为
5. 发布 v0.2.0
