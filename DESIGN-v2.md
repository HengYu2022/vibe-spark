# Design: Vibe Spark — Vibe Coding 灵感导航员

Generated by /office-hours on 2026-04-01
Branch: main
Repo: vibe-spark
Status: DRAFT
Mode: Builder
Supersedes: DESIGN.md（web app 版，已废弃）

## 问题描述

Vibe coder 最大的卡点是「不知道做什么」。有 code 的冲动，缺方向。

现有工具的问题：
- **app-ideas**（90k star）：静态列表，没有引导，没有上下文感知
- **SEED skill**（108 star）：帮你细化 idea，但前提是你已经有方向
- **/office-hours**（gstack 内置）：通用产品探索，不针对 vibe coding 场景优化
- **直接问 Claude「给我个灵感」**：随机生成，没有引导，没有记忆，质量不稳定

断层在于：没有一个工具专门帮 vibe coder **从零开始找到方向，并且越用越懂你**。

## 什么让它酷

Vibe Spark 是一个 Claude Code skill，本质是 **vibe coding 场景的垂直 office-hours pro max**。

三个核心能力：
1. **上下文感知启动** — 不是冷启动问「你想做什么」，而是先检索你之前的对话、项目文件、git 历史，发现你已经在想的方向，直接接过话头
2. **灵感库引导** — 不是开放式瞎聊，而是基于高质量策展灵感库帮你快速收敛到一个值得做的方向
3. **持久化记忆** — 记住你的偏好、看过的灵感、做过的项目，第 N 次使用时推荐越来越精准

**跟直接问 Claude 的根本区别：** Claude 不记得你，每次都是冷启动。Vibe Spark 越用越懂你。

**跟 /office-hours 的区别：** /office-hours 是通用的，问的是「你的用户是谁」「需求验证了吗」这种通用问题。Vibe Spark 问的是「你最近对什么技术感兴趣」「你有多少时间」「你想做给自己用还是别人用」，完全针对 vibe coder 找灵感这件事优化。

**设计灵感来源：** 我们自己的设计对话就是 beta 测试。聊了 1 小时，换了 4 次方向才收敛。Vibe Spark 的目标是把这 1 小时压缩到几分钟，因为它预装了 vibe coding 领域知识，手里有高质量灵感弹药。

## 约束

- MVP 可以多花几天，不限于一个周末
- 开源，MIT 协议
- Claude Code skill 形态（SKILL.md + 灵感库 + 持久化数据）
- 项目代码和灵感库在项目目录内，用户持久化数据在 `~/.vibe-spark/`（跟随用户）
- 中文优先（v2 考虑双语）

## 前提

1. 「不知道做什么」是 vibe coder 最大的卡点
2. 形态必须是 Claude Code skill
3. 核心差异化是「上下文感知 + 灵感库引导 + 快速收敛」三件套
4. 价值在于引导过程，不在于推荐结果。但过程必须比直接聊天快 10 倍
5. 灵感库是引导对话的弹药库，不是终点产品
6. 输出格式不重要（design doc / PROJECT_BRIEF 都行），关键是让 Claude Code 能接着做

## 方案对比

### 方案 A：灵感导航员（无持久化）
- 摘要：上下文感知 + 灵感库引导 + 快速收敛，但不记住用户
- 工作量：M（一个周末）
- 风险：低
- 优点：实现简单，核心体验到位
- 缺点：每次冷启动，跟直接问 Claude 的差距不够大

### 方案 B：灵感自动贩卖机
- 摘要：纯匹配推荐，没有引导对话
- 工作量：S
- 风险：中
- 优点：极简
- 缺点：没有差异化

### 方案 C：灵感导航员 + 持久化（已选）
- 摘要：A 的全部能力 + 用户偏好记忆 + 灵感历史追踪 + 越用越精准
- 工作量：M+（多约 1 天）
- 风险：低
- 优点：持久化让 Vibe Spark 越用越懂你，这是直接问 Claude 永远做不到的
- 缺点：需要设计持久化数据结构
- 复用：gstack 的 learnings 持久化模式（JSONL + 本地目录）

## 推荐方案

**方案 C：灵感导航员 + 持久化。**

---

## SKILL.md 结构

```yaml
---
name: vibe-spark
description: 当用户不知道做什么项目、需要灵感、想找 side project idea、说「不知道做什么」时使用。Vibe coding 灵感导航员，帮你从零找到值得做的方向。
---
```

## 完整工作流

```
/vibe-spark

═══ Phase 0：上下文感知（自动，无交互）═══

通过 Bash 和 Read 工具执行以下检测（SKILL.md 中用自然语言指令 Claude 执行）：

1. 读取持久化数据
   ```bash
   mkdir -p ~/.vibe-spark
   cat ~/.vibe-spark/profile.jsonl 2>/dev/null | tail -10
   ```
   解析最近 10 条记录，提取：
   - 用户偏好：喜欢的方向、常用技术栈、通常可用时间
   - 灵感历史：看过哪些、否掉哪些、选了哪些
   - 上次使用时间

2. 检测当前项目上下文
   - 用 Glob 检测 package.json / Cargo.toml / pyproject.toml（提取技术栈）
   - 用 Bash `ls` 检测当前目录是否为空（空 = 找新项目）

（注：不检索 session 对话历史，Claude Code skill 启动时没有这个能力。
  跨 session 记忆完全靠 profile.jsonl。）

═══ Phase 1：热启动 or 冷启动 ═══

**热启动条件：** profile.jsonl 存在且最近一条记录在 30 天内。

【热启动】：
→ 直接接过话头，引用上次的选择
  例：「你上次对 CLI 工具类灵感感兴趣，选了「时间追踪器」。
       想继续那个方向，还是换个新的？」

【冷启动】profile.jsonl 不存在或最近无记录：
→ AskUserQuestion（Claude Code 内置交互工具，SKILL.md 中用自然语言指令即可触发）：
  - 你想做什么类型的项目？有多少时间？
    （单次提问，选项从灵感库标签生成 + 自由输入。
     不要连问 3 个问题，保持轻快。）

═══ Phase 2：灵感库引导 ═══

1. 匹配逻辑（两步过滤）：
   Step A — 文件发现 + 标签过滤：
            用 Glob 获取 ideas/**/*.md 文件路径列表，
            用 Bash `head -8` 批量读取每个文件的 frontmatter（YAML 部分），
            按用户选择的 difficulty + tags 过滤。
            （30 条灵感约 30 次 head 调用，可接受。若库超过 50 条，
            改为维护一个 ideas/index.json 汇总文件加速过滤。）
   Step B — Claude 语义排序：将过滤后的候选集（标题+一句话+wow）
            拼入 prompt，让 Claude 选出最匹配用户偏好的 3 个
   - 排除 profile.jsonl 中已否掉的灵感 ID
   - 若回访用户，优先推未展示过的灵感
   - 若过滤后不足 3 个，放宽条件（只保留 difficulty 过滤）

2. 展示 3 个灵感（标题 + 一句话 + wow factor）
   + 第 4 个选项「我有自己的想法」

3. 用户选择后：
   - 选了一个灵感 → Phase 3
   - 「我有自己的想法」→ 用户输入想法
     → Claude 在灵感库中搜索相关灵感作为参考展示
     → 引导对话（限 2 轮）：
       第 1 轮：「这个想法最酷的版本是什么？做到什么程度算 MVP？」
       第 2 轮：「技术栈有偏好吗？大概想花多少时间？」
     → Phase 3
   - 「换几个」→ 重新匹配，排除已展示（最多换 2 轮）
   - 换完 2 轮仍不满意 → 问用户：「要放宽条件看更多，还是说说你自己的想法？」
     → 放宽条件 → 去掉 tags 过滤，只保留 difficulty，再推 3 个
     → 自己说 → 转入「我有自己的想法」路径

═══ Phase 3：锁定方向 + 输出 ═══

1. 确认最终方向（一句话总结 + 核心功能 3-5 条）
2. AskUserQuestion：「这个方向锁了？」
   - 确认 → 输出文档到当前目录
   - 想调整 → 微调后重新确认
3. 输出格式根据上下文自动选择：
   - 空目录 → 输出 PROJECT_BRIEF.md（简洁，直接开工用）
   - 已有项目 → 输出 SPARK_IDEA.md（灵感描述，不覆盖现有文件）
4. 提示用户：「方向锁定了。直接跟 Claude Code 说
   '按照 XXX.md 开始做'就行。」

═══ Phase 4：持久化 ═══

通过 Bash 追加一行 JSON 到 profile.jsonl：
```bash
mkdir -p ~/.vibe-spark
echo '{"ts":"...","shown":[...],"rejected":[...],"selected":"...","preferences":{...}}' >> ~/.vibe-spark/profile.jsonl
```

记录本次交互：
- 用户选择/否掉了哪些灵感
- 最终锁定的方向
- 用户的偏好信号（时间、方向、技术栈）
- 时间戳

「越用越精准」的机制：Phase 0 读取最近 10 条 profile 记录，
拼入 SKILL.md 的 prompt 上下文。Claude 在 Phase 2 匹配时会
自动考虑这些历史偏好（例如：用户连续 3 次选 CLI 类灵感，
下次优先推 CLI 方向的新灵感）。不需要单独的 preferences.json，
Claude 直接从 profile.jsonl 原始记录中理解偏好。
```

## 灵感条目模板

每条灵感用 Markdown + YAML frontmatter：

```yaml
---
title: 个人 CLI 时间追踪器
tags: [cli, productivity]
difficulty: beginner
time: 2-4h
stack: [Node.js, SQLite]
wow: "你以为你在工作，其实你在摸鱼——数据不会说谎"
---
## 一句话
在终端里追踪你每天真正花在每个项目上的时间。

## 为什么值得做
有真实使用场景，数据是你自己的，做完自己每天都会用。

## 核心功能
1. `start <project>` / `stop` 命令控制计时
2. `report` 命令生成今日/本周时间分布
3. 本地 SQLite 存储，数据属于你

## 第一步
创建 `tracker.js`，实现 `start` 和 `stop` 两个命令，
把时间戳写入本地 JSON 文件。

## 扩展方向
- 周报 Markdown 自动生成
- 终端 UI 美化（用 blessed 或 ink）
```

## 标签体系

**方向：** cli, web, api, game, tool, creative, data-viz, ai, browser-ext, mobile
**难度：** beginner（几小时）, intermediate（一个周末）, advanced（一周+）
**技术栈：** react, vue, node, python, rust, go, swift 等

## 持久化数据结构

存储在 `~/.vibe-spark/`（用户 home 目录，不在项目目录内，因为持久化数据跟随用户而非项目）：

```
~/.vibe-spark/
└── profile.jsonl      # 用户交互历史（每次使用追加一行）
```

不需要单独的 preferences.json。Claude 直接读 profile.jsonl 最近 10 条记录，从原始数据中理解用户偏好。简单可靠。

profile.jsonl 每行格式：
```json
{
  "ts": "2026-04-01T12:00:00Z",
  "shown": ["time-tracker", "emoji-password", "ai-weekly-report"],
  "rejected": ["emoji-password"],
  "selected": "time-tracker",
  "user_input": null,       // 用户走「我有自己的想法」路径时记录输入内容，否则 null
  "preferences": {
    "time": "几小时",
    "direction": "cli",
    "stack": "node"
  }
}
```

## 灵感引导对话的设计原则

1. **热启动优先** — 有上下文就直接接话，不要冷启动问一堆问题
2. **最多 3 轮对话收敛** — 用户耐心有限，不像我们的设计对话走了 6 轮换了 4 次方向
3. **灵感库是弹药不是菜单** — 不要列一堆让用户翻，基于上下文精准推 3 个
4. **永远有自由输入出口** — 每个 AskUserQuestion 都允许用户说自己的想法
5. **否掉的灵感不再出现** — 持久化让推荐越来越精准
6. **引导不是盘问** — 问题要轻快。不问「你的用户是谁」，问「做给自己用还是给别人用」

## 灵感库来源和策展流程

1. 从 florinpop17/app-ideas（90k star）中筛选适合 vibe coding 的项目
2. 用 AI 改写成模板格式，加入 wow factor 和具体的「第一步」
3. 补充 AI 应用、CLI 工具、浏览器扩展等 app-ideas 覆盖不到的方向
4. 人工审核每一条，标准：读完能立刻开工

## 项目结构

```
vibe-spark/
├── SKILL.md              # Skill 定义（frontmatter + 完整工作流 prompt）
├── DESIGN-v2.md          # 本文档（正式设计文档）
├── README.md             # 安装和使用说明
├── LICENSE               # MIT
├── ideas/                # 策展灵感库
│   ├── cli/
│   │   ├── time-tracker.md
│   │   └── ...
│   ├── web/
│   │   ├── emoji-password.md
│   │   └── ...
│   ├── ai/
│   │   ├── weekly-report-generator.md
│   │   └── ...
│   ├── game/
│   ├── tool/
│   └── creative/
└── examples/             # 示例输出
    ├── PROJECT_BRIEF.md  # PROJECT_BRIEF 样例
    └── SPARK_IDEA.md     # SPARK_IDEA 样例
```

运行时持久化数据存在 `~/.vibe-spark/`（跟随用户，不跟随项目）。

## 文件命名规范

灵感条目文件名用英文 slug（time-tracker.md），内容用中文。

## 执行环境假设

- Claude Code skill 可通过 Bash、Read、Glob、Write 工具执行文件操作
- 用户在 Claude Code 中运行（非沙箱环境），可读写 `~/.vibe-spark/`
- AskUserQuestion 是 Claude Code 内置交互工具，SKILL.md 中用自然语言指令即可触发
- 灵感匹配质量依赖 Claude 的语义理解，不使用向量搜索。匹配不准时 fallback 为随机推荐 + 自由输入

## 错误处理

- `~/.vibe-spark/` 不存在 → Phase 0 的 `mkdir -p` 自动创建
- profile.jsonl 不存在或为空 → 走冷启动路径
- profile.jsonl 格式损坏 → 忽略损坏行，走冷启动路径
- 灵感库为空或过滤后无结果 → 直接进入「我有自己的想法」路径
- 当前目录无读权限 → 跳过项目上下文检测，直接问用户

## 成功标准（工程验收）

- 灵感库 30+ 条高质量灵感，覆盖 5+ 方向标签和 3 个难度
- 上下文感知能正确检测技术栈（读 package.json 后推荐 Node.js 相关灵感）
- 持久化验收场景：
  - 第一次使用 → 冷启动 → 选择灵感 → profile.jsonl 有记录
  - 第二次使用 → 热启动 → 正确引用上次选择 → 不重复推荐已否掉的灵感
  - 第三次使用 → 偏好影响排序（连续选 CLI 后优先推 CLI 新灵感）
- 完整工作流跑通（启动 → 引导 → 锁定 → 输出 → 持久化）

## 市场目标（非工程要求）

- GitHub star 100+（第一个月）
- 被 awesome-claude-skills 或类似列表收录
- 有人用完后直接开始写代码

## 分发计划

- GitHub 开源仓库，MIT 协议
- 安装方式：`git clone` 到 Claude Code 的 skill 目录
  （Claude Code 官方文档：skill 放在项目的 `.claude/skills/` 或用户级 `~/.claude/skills/`。
   推荐 `~/.claude/skills/vibe-spark/`，全局可用。发布前需验证实际加载路径。）
- 提交到 awesome-claude-skills 列表
- Twitter/X 发布，附 GIF 演示
- vibe coding 社群分享

## 下一步

1. **定义灵感条目模板** — 用 3 个具体灵感验证模板格式是否好用
2. **策展灵感库** — 30 条起步，AI 辅助 + 人工审核
3. **写 SKILL.md** — 实现完整工作流（上下文感知 + 引导 + 输出 + 持久化）
4. **测试持久化** — 验证多次使用的体验是否逐步提升
5. **发布** — README + awesome-claude-skills 提交

## 从我们的对话中借鉴的设计模式

| 我们的对话（反面教材） | Vibe Spark 怎么避免 |
|----------------------|-------------------|
| 聊了 1 小时换了 4 次方向 | 灵感库作为弹药，几轮引导就锁定方向 |
| 冷启动问通用问题 | 上下文感知热启动，直接接过话头 |
| 每次都是全新对话 | 持久化记忆，越用越精准 |
| 开放式探索没有边界 | 灵感库限定候选范围，引导收敛 |
| 「跟直接问 Claude 有什么区别？」 | 记忆 + 策展 + 引导三件套是差异化 |

## 关于你的思考方式

- 你说「去搜索一下目前 GitHub 同类高星的产品，取其所长攻其所短」— 先看市场再动手，不闷头做。
- 你追问「这个 skill 和直接在 Claude Code 里让给个灵感有区别吗」— 逼自己想清楚不可替代性。
- 你说「真实有效的，而不是没有价值的花瓶」— 在意用户拿到灵感后真的能开工。
- 你指出「别人都有 office-hours 了为啥用我们的」— 看到了通用工具 vs 垂直工具的竞争逻辑。
- 你说「如果像我们一样来回 6 轮，耐心早就被磨掉了」— 从用户视角审视自己产品的体验缺陷。
- 你坚持「整个工作流都可以参考 gstack」— 复用已被验证的模式，而不是从零发明。
